import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationView {
            Text("设置")
                .navigationTitle("设置")
        }
    }
}

#Preview {
    SettingsView()
} 